
class yeast:
    def __init__(self, flour: str, water: str, bake: list):

        self.flour = flour 
        self.water = water 
        self.bake = bake 
    def bake(): 
            return "You bake the + self.flour + self.water"

  # Define an abstract class 
    class bread(): 
        
        def bake(int): 
         pass # This is an abstract method, no implementation here. 

        # Concrete subclass of yeast 
        class yeast(): 

                def bake(int): 
                     return "bake"
    # Create instanct of bread 
    yeast = bread()
    print(yeast.bake())
    Output: bread

    def __str__(self, flour: str, water: str, bake: int):
        str.flour = flour 
        str.water = water 
        str.bake = bake  
        
        def bake():
            return "You bake the + self.flour + self.water" 
        
#yeast 
def yeast(*args): 
    return sum(args)

